## 🐛 Bugs

- fix(cart): rendre lines[].associations déclaratif sur PATCH /carts/{id}

- fix(auth): remove the per-IP throttle on oauth/token

- fix(FFFSsoUserService): prevent user group loss during SSO sync failure 1218444209993087