## 🐛 Bugs

- fix(prestashop): guard against a connector user_group with no resolved user or shop

- fix(shipping): make Country::getByIso2 case-insensitive

- fix(user_group): check the acting user for seller-feature access, not every group member

- Fix sentry error order seller nonexistent when deleting cart line - 1216901390380830

- Fix SendCloud ModuleLog message: 'deliveryMode' not replaced


## 🛠️ Improvements

- Improvement: register module service providers on resolution, and compile a module manifest

- test(shipping): cover getByIso2 fallback callers in shipping zone resolution

- fix(product): loosen flaky performance timing assertion in ProductQueryCountTest